import { CreateExpenseInterface } from './create-expense-interface';

describe('GroupIdInterface', () => {
  it('should create an instance', () => {
    expect(new CreateExpenseInterface()).toBeTruthy();
  });
});
